{
  "version": 2,
  "framework": "vue",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/vue",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
